{
  "name": "go-aws-starter",
  "private": true,
  "scripts": {
    "start": "lerna run start --parallel",
    "test:api": "lerna run test:api",
    "build": "lerna run build --parallel",
    "deploy": "lerna run deploy --parallel",
    "deploy:frontend": "lerna run deploy:frontend --parallel",
    "install": "lerna bootstrap && lerna run local:build && lerna run local:add"
  },
  "devDependencies": {
    "lerna": "^3.10.7"
  }
}
